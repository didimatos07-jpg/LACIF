rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read access to site configurations
    match /config/{configId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
